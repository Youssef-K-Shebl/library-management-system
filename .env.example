# Application
NODE_ENV=development
PORT=3000

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=library_db

# Auth (bonus)
AUTH_USERNAME=admin
AUTH_PASSWORD=secret

# Rate Limiting (bonus)
THROTTLE_TTL=60
THROTTLE_LIMIT=10
